// Elastic ES|QL — generated by log-analyzer from Sigma
from * metadata _id, _index, _version | where event_type=="failed_login"
| eval timebucket=date_trunc(10minutes, @timestamp) | stats event_count=count() by timebucket, source.ip
| where event_count >= 5
