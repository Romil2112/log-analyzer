// Elastic ES|QL — generated by log-analyzer from Sigma
from * metadata _id, _index, _version | where event_type=="connection"
| eval timebucket=date_trunc(5minutes, @timestamp) | stats value_count=count_distinct(destination.port) by timebucket, source.ip
| where value_count >= 20
