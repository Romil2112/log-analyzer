// Microsoft Sentinel KQL — generated by log-analyzer from Sigma
event_type =~ "connection"
| summarize value_count = dcount(DstPortNumber) by bin(TimeGenerated, 5m), SrcIpAddr
| where value_count >= 20
